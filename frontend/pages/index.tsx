import type { NextPage } from "next";
import { Button, Container } from "react-bootstrap";

const Home: NextPage = () => {
    return (
        <Container>
            <h1>Hello, World!</h1>
            <Button>BUTTON</Button>
        </Container>
    );
};

export default Home;
